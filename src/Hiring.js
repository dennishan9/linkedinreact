import React from 'react'
import { CheckingDate } from './CheckingDate'

export const Hiring = () =>
	<div>
		<p>It's {CheckingDate()} and the library is hiring. Go to www.library.com/jobs for more.</p>
	</div>
