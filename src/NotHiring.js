import React from 'react'
import { CheckingDate } from './CheckingDate'

export const NotHiring = () =>
	<div>
		<p>It's {CheckingDate()} and the library is not hiring. Check back later for more info.</p>
	</div>