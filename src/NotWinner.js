import React from 'react'
import { CheckingDate } from './CheckingDate'

export const NotWinner = () =>
    <div>
        <p>It's {CheckingDate()} and you're not the winner, better luck next time!</p>
    </div>